// @ts-check
// 타입스크립트 파일에 자바스크립트 파일 확인하라는 표시

/**
 * Initializes the project
 * @param {object} config
 * @param {boolean} config.debug
 * @param {string} config.url
 * @returns {boolean}
 */
export const init = (config) => {
  return true;
};
/**
 * Exits the program
 * @param {number} code
 * @returns {number}
 */
export const exit = (code) => {
  return code + 1;
};

